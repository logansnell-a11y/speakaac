[build]
  publish = "."
  functions = "netlify/functions"

# ── Required environment variables (set in Netlify → Site → Environment Variables) ──
# ANTHROPIC_API_KEY        = sk-ant-...        (Anthropic Console → API Keys)
# SUPABASE_URL             = https://ymljgpaublxjazfgcefz.supabase.co
# SUPABASE_SERVICE_ROLE_KEY = eyJ...           (Supabase → Project Settings → API → service_role)
